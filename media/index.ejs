<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />

    <meta
      http-equiv="Content-Security-Policy"
      content="default-src 'none'; style-src <%= cspSource %>; script-src 'nonce-<%= nonce %>';"
    />

    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <link href="<%- styleResetUri %>" rel="stylesheet" />
    <link href="<%- styleVSCodeUri %>" rel="stylesheet" />
    <link href="<%- styleMainUri %>" rel="stylesheet" />

    <title>Regex Context</title>
  </head>
  <body>
    <h3>context</h3>
    <button id="btnAddContext">add context</button>
    <div>
      <label for="chkPattern">is active?</label>
      <input type="checkbox" id="chkPattern" checked="true" />
    </div>

    <div id="contextCtr"></div>

    <h3>find / replace</h3>
    <div class="flex">
      <input type="text" id="txtFind" placeholder="Find" />
      <input type="checkbox" id="checkShowFind" checked="true" />
    </div>
    <div class="flex">
      <input type="text" id="txtReplace" placeholder="Replace" />
    </div>
    <div>
      <button id="btnReplace">replace</button>
    </div>
    <h3>extract</h3>
    <div class="flex-col">
      <p>extract each matching context as a line in a new document</p>
      <button id="btnExtractCtx">extract context...</button>
    </div>
    <div class="flex-col">
      <p>
        extract each matching find item (inside a context) as a line in a new
        document
      </p>
      <button id="btnExtractFind">extract finds...</button>
    </div>

    <script nonce="<%= nonce %>">
      var exports = {};
    </script>
    <script nonce="<%= nonce %>" src="<%- scriptUri %>"></script>
  </body>
</html>
